# AGENTS.md

## Project

- **Name**: paytest-routing
- **Package manager**: pnpm 11.24.0 (corepack-compatible, set in `packageManager` field)
- **Module system**: ESM (`"type": "module"` in package.json)
- **Primary dependency**: `http-proxy-middleware` v4.2.0 (proxy/routing library)

## Commands

```bash
pnpm install          # install dependencies
```

No test, lint, or build scripts are configured yet. The default `test` script is a placeholder (`echo "Error: no test specified" && exit 1`).

## Code Style

- Use ESM syntax (`import`/`export`)
- Follow standard JavaScript conventions

## Notes

- No source files exist yet — this is a fresh project scaffold.
- The `improve-codebase-architecture` skill is installed locally (`.agents/skills/`).
